<Window x:Class="Arent3d.Architecture.Routing.AppBase.Forms.SelectWiringDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Arent3d.Architecture.Routing.AppBase.Forms"
        mc:Ignorable="d" x:Name="SelectWiring"
        Title="Wiring Information Dialog" Height="500" Width="800"
        d:DataContext="{d:DesignInstance {x:Type local:DesignSelectWiringViewModel}, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding WiringList}" SelectedItem="{Binding SelectedWiring}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="100" IsReadOnly="True" Binding="{Binding IdOfToConnector}" />
                <DataGridTextColumn Header="Route Name" Width="150" IsReadOnly="True" Binding="{Binding RouteName}" />
                <DataGridTextColumn Header="Wire Type" Width="150" IsReadOnly="True" Binding="{Binding WireType}" />
                <DataGridTextColumn Header="Wire Size" Width="150" IsReadOnly="True" Binding="{Binding WireSize}" />
                <DataGridTextColumn Header="Wire strip" Width="150" IsReadOnly="True" Binding="{Binding WireStrip}" />
                 
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="100" Content="Change Info" Margin="10"
                    Command="{Binding ChangeWiringInfoCommand}"
                    CommandParameter="{Binding ElementName=SelectWiring}" />
            <Button Width="100" Content="Save" Margin="10"
                    Command="{Binding SaveWiringCommand}"
                    CommandParameter="{Binding ElementName=SelectWiring}" />
            <Button Width="100" Content="Cancel" Margin="10"
                    Command="{Binding CancelCommand}"
                    CommandParameter="{Binding ElementName=SelectWiring}" />
        </StackPanel>


    </Grid>
</Window>